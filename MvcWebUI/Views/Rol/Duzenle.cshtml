@model RolModel
@{
    ViewData["Title"] = "Rol Düzenle";
}
<div class="container login-page">
    <div class="row">
        <div class="col-12">
            <h2>@ViewData["Title"]</h2>
            <form asp-action="Duzenle">
                @Html.AntiForgeryToken()
                <div asp-validation-summary="All" class="text-danger"></div>
                <div class="form-outline mb-4">
                    <input type="hidden" asp-for="Id" />
                    <input asp-for="Adi" type="text" id="form2Example1" class="form-control" />
                    <label asp-for="Adi" class="form-label" for="form2Example1"></label>
                    <span asp-validation-for="Adi" class="text-danger"></span>
                </div>
                <button type="submit" class="btn btn-primary btn-block mb-4" value="Kayit">Rol Duzenle</button>
            </form>
        </div>
    </div>
</div>